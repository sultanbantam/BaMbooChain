version: '3.8'

services:
  agri-tech-service:
    build: .
    container_name: bmc-agri-tech-service
    ports:
      - "5001:5001"
    environment:
      - PORT=5001
      - NODE_ENV=development
    volumes:
      - ./data:/app/data
    restart: unless-stopped
